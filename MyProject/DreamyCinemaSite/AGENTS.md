## Planning With Files

复杂任务或多步骤任务请使用 `planning-with-files` skill 管理。

开始前先创建或读取项目目录下的：
- `task_plan.md`
- `findings.md`
- `progress.md`

按阶段执行。每次完成有意义的修改后更新 `progress.md`；重要发现写入 `findings.md`；阶段状态变化时同步更新 `task_plan.md`。