<Window x:Class="CodexApiProxy.ProviderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="API 提供商" Height="280" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="名称:" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtName" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="API 地址:" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtApiUrl" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="API Key:" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <Grid Grid.Row="2" Grid.Column="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <PasswordBox Grid.Column="0" x:Name="txtApiKey" VerticalAlignment="Center"/>
            <TextBox Grid.Column="0" x:Name="txtApiKeyPlain" VerticalAlignment="Center"
                     Visibility="Collapsed" VerticalContentAlignment="Center"/>
            <ToggleButton Grid.Column="1" x:Name="btnEye" Width="28" Height="28" Margin="4,0,0,0"
                          Click="BtnEye_Click" ToolTip="显示/隐藏密码">
                <TextBlock x:Name="txtEye" Text="&#x1F441;" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </ToggleButton>
        </Grid>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="模型:" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="txtModel" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Width="75" Height="30" IsDefault="True" Click="BtnOk_Click" Margin="0,0,10,0"/>
            <Button Content="取消" Width="75" Height="30" IsCancel="True" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
